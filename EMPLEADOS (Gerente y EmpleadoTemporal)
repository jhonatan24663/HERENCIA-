class Empleado:
    def __init__(self, nombre, salario, cargo):
        self.nombre = nombre
        self.salario = salario
        self.cargo = cargo
    
    def mostrar_info(self):
        print(f"Nombre: {self.nombre}")
        print(f"Cargo: {self.cargo}")
        print(f"Salario: ${self.salario}")

class Gerente(Empleado):
    def __init__(self, nombre, salario, departamento):
        super().__init__(nombre, salario, "Gerente")
        self.departamento = departamento
    
    def aumento_salario(self):
        return self.salario * 0.20
    
    def mostrar_info(self):
        super().mostrar_info()
        print(f"Departamento: {self.departamento}")
        print(f"Aumento: ${self.aumento_salario():.2f}")

class EmpleadoTemporal(Empleado):
    def __init__(self, nombre, salario, meses):
        super().__init__(nombre, salario, "Empleado Temporal")
        self.meses = meses
    
    def aumento_salario(self):
        return self.salario * 0.05
    
    def mostrar_info(self):
        super().mostrar_info()
        print(f"Duración del contrato: {self.meses} meses")
        print(f"Aumento: ${self.aumento_salario():.2f}")

